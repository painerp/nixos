name: Update Docker Mappings

on:
  push:
    paths:
      - 'containers/**.nix'
  schedule:
    - cron: '0 0 * * 0'
  workflow_dispatch:

jobs:
  update-mappings:
    runs-on: ci-docker
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Generate Docker mappings
        run: python .gitea/scripts/update_docker_renovate.py

      - name: Commit mapping file
        uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore: update docker image mappings in renovate.json'
          file_pattern: 'renovate.json'